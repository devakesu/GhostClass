# npm configuration for GhostClass

# Audit configuration: report all vulnerabilities but set moderate threshold
# In CI, use: npm audit --omit=dev  (see docs/DEVELOPER_GUIDE.md#known-issues)
# This prevents failures on known dev-tool vulnerabilities while catching production issues
audit-level=moderate
