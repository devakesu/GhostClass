// CSRF Constants (safe for client components)
// src/lib/csrf-constants.ts

/**
 * CSRF token header name for client-side requests
 */
export const CSRF_HEADER = "x-csrf-token";

/**
 * CSRF token cookie name
 */
export const CSRF_TOKEN_NAME = "gc_csrf_token";
